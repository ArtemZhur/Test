schemas:
  ImageSimilarityResponse:
    type: array
    items:
      type: object
      description: course data
      allOf:
        - type: object
          required:
            - id
            - name
            - url
